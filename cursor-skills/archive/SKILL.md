---
name: archive
description: View archive or archive a creature
---

Usage:
- `/archive` — view archived creatures
- `/archive [id]` — archive a creature by ID

Parse the argument for an optional creature ID.

Call the compi `archive` MCP tool with the parsed `id` (optional string).

The result is displayed in the visual panel above. Respond with a brief one-line summary.

Do NOT output the raw tool response.
